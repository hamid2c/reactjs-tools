program proj_dir -o /tmp/build

x1. add home to package.json
x2. copy webpack config to proj_dir
x3. npm run build in proj_dir
x4. js-beautify on output dir
x5. move the build to output directory
x5.5. package.json
x6. update README
x7. instrument the generated website with jalangi
    -x check JALANGI_HOME env and print 
    -x should we cd to JALANGI_HOME? no
x. incorporate janalgi-inst script to pack.js
. update README, USAGE  and default output directory, etc
. mention JALANGI_HOME IN README
. talk about pipeline.analyses in README
